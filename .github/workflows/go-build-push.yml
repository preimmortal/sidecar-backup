name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: Test
      run: go test -v ./...

    #- name: Build amd64
    #  run: env GOOS=linux GOARCH=amd64 go build -v -o sidecar-backup.amd64 cmd/sidecar-backup/main.go

    #- name: Build arm64
    #  run: env GOOS=linux GOARCH=arm64 go build -v -o sidecar-backup.arm64 cmd/sidecar-backup/main.go

    #- name: Build armv7
    #  run: env GOOS=linux GOARCH=arm GOARM=7 go build -v -o sidecar-backup.armv7 cmd/sidecar-backup/main.go

    - name: Docker meta
      id: meta
      uses: docker/metadata-action@v3
      with:
        images: ghcr.io/preimmortal/sidecar-backup
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=raw,value={{sha}}-{{date 'YYYY-MM-DD'}}
          type=raw,value=latest,enable=${{ endsWith(github.ref, github.event.repository.default_branch) }}
    
    - name: Set up QEMU
      uses: docker/setup-qemu-action@v1
      with:
        platforms: all

    - name: Login to GHCR
      uses: docker/login-action@v2
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}


    - name: Set up Docker Buildx
      id: buildx
      uses: docker/setup-buildx-action@v1

    - name: Build image and push
      id: docker_build
      uses: docker/build-push-action@v2
      with:
        builder: ${{ steps.buildx.outputs.name }}
        context: .
        file: ./Dockerfile
        platforms: linux/amd64,linux/arm64/v8,linux/arm/v7
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.tags }}
    
    - name: Image digest
      run: echo ${{ steps.docker_build.outputs.digest }}